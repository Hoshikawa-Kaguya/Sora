<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net5.0</TargetFramework>
    <RunPostBuildEvent>OnOutputUpdated</RunPostBuildEvent>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Sora\Sora.csproj" />
  </ItemGroup>

  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <Exec Command="del $(OutDir)*.deps.json&#xD;&#xA;del $(OutDir)*.runtimeconfig.dev.json&#xD;&#xA;if &quot;$(ConfigurationName)&quot;==&quot;Release&quot; del $(OutDir)*.pdb&#xD;&#xA;rd /s/q  $(OutDir)ref" />
  </Target>

</Project>
